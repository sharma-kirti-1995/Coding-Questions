int[] arr = {2,5,6,4,7,1,9}; //7

int firstMax = Integer.MIN_VALUE;
int secondMax = Integer.MIN_VALUE;
for(int i=0;i<arr.length;i++) {
  if(a[i] > firstMax ) { firstMax = a[i]; }  
}

for(int i=0;i<arr.length;i++) {
  if(a[i] > secondMax && a[i]!=firstMax ) { secondMax = a[i]; }
}
